node_modules
.next
*.tsbuildinfo
.DS_Store
dist
out
coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 运行时 JSON（业务/用户数据，勿提交；本地首次运行由接口或 storage-local 按需生成）
/data/*.json

# 本地上传的封面/媒体（勿提交仓库；生产环境用 S3/R2）
public/uploads/*
!public/uploads/.gitkeep
